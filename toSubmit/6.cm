/* This program tests 3 various syntactic and semantic errors: */
/* Type checking for if condition, invalid expression, type mismatch in assignment */

void foo(void) {
    return;
}

void main(void) {
    bool test;
    int variable;

    test = true;

    if(foo()) {       /* Semantic Error: Test condition must be int or bool. */
        output(2);
    }

    foo2flksjd);      /* Syntactic Error: Invalid expression */

    test = 100;       /* Semantic Error: Type mismatch in assignment */

}